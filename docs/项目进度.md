# 渡风电商平台 · 项目进度与上下文速览

> 本文件用于快速理解仓库现状、运行方式与本轮变更，供开发/后续会话参考，避免重复探索消耗 token。
> 项目基线：`docs/渡风电商平台需求分析文档.md`；本文件不替代需求文档，只记录“当前落地状态”。

## 1. 技术栈与架构

| 层 | 技术 |
| --- | --- |
| 后端 | Spring Boot 3.x · JDK 17 · MyBatis-Plus · Flyway · JWT + Redis 会话黑名单 |
| 前端 | Vue 3（`<script setup>`）· Vite · Pinia · Vue Router 4 · Element Plus |
| 数据库 | MySQL 8（云服务器） |
| 缓存 | Redis 7（云服务器） |
| 支付 | 支付宝沙箱（生产已启用真实网关，`ALIPAY_ENABLED=true`；默认关闭模拟回调） |

- 包结构：`com.dufeng.<module>`，模块按领域拆分（user / goods / order / payment / merchant / admin / review / cart / address）。
- 分层：`Controller → Service → Mapper`，统一返回 `{ code: 0, message, data }`（`code=0` 成功），全局 `@RestControllerAdvice`。
- 接口前缀：前台 `/api/portal`，商家 `/api/merchant`，平台 `/api/admin`。

## 2. 目录速览

```text
backend/src/main/java/com/dufeng/...   后端业务代码
backend/src/main/resources/db/migration   Flyway 迁移（V1 建表 / V2 审计日志 / V3 初始化数据）
backend/start-local.sh                  本地联调脚本（连云服务器，已被 gitignore）
frontend/src/api                          Axios 封装（admin / merchant / home / goods ...）
frontend/src/views                        页面（portal / merchant / admin）
frontend/public/img                      商品图 goods-1~15.jpg、banner-1~3.jpg
deploy/                                   云服务器 Docker Compose & 部署文档
docs/                                     需求分析、支付宝沙箱接入、本进度文件
```

## 3. 运行方式

### 后端（本地连云服务器中间件）
```bash
# 连接参数在 backend/start-local.sh（gitignore，不入库），指向云服务器 MySQL/Redis
cd backend && bash start-local.sh          # 等价 mvn spring-boot:run，profile=dev
```
- 数据库连接（host / 库 / 账号见 `start-local.sh`）：MySQL `119.29.167.133:3306`，库 `dufeng`；Redis 同机 `6379`。
- 首次启动 Flyway 自动执行 `V1__init.sql`、`V2__audit_log.sql`、`V3__init_seed_cloud.sql`。

### 前端
```bash
cd frontend && npm install && npm run dev   # http://localhost:5173
```

### 演示账号（来自云库，V3 迁移播种）
| 角色 | 账号 | 密码 |
| --- | --- | --- |
| 平台管理员 | admin | admin123 |
| 入驻商家 | merchant | merchant123 |
| 普通用户 | demo | demo123 |

## 4. 数据库初始化数据（本轮关键变更）

- **原状**：初始化数据写死在 `com.dufeng.config.DataInitializer`（仅 dev，幂等播种账号/类目/品牌/商家/店铺/商品/SKU）。
- **现状**：已删除 `DataInitializer.java`；初始化数据改由 **`V3__init_seed_cloud.sql`** 提供，该脚本是**从云服务器数据库导出**的真实数据（账号、类目、品牌、商家、店铺、15 个商品、SKU），使用 `INSERT IGNORE`（幂等，可重复执行），并保留云库的 BCrypt 密码哈希。
- 生产/云库已有数据时，V3 重复执行不会改动现有数据（`INSERT IGNORE` 直接跳过）。
- 如需重置为空库，只需让 Flyway 依序执行 V1→V2→V3。

> ⚠️ `t_goods` 中 `id=goods-1` 的 `images` 字段原库为异常 JSON（`["url":...]`），前端 `parseImages` 有 try/catch 兜底到 `mainImage`，详情页可正常显示；如需修复可改为 `[{"url":"/img/goods-1.jpg"}]`。

## 5. 商品图片映射（goods-N.jpg ↔ 商品 ↔ 云库 final800 图）

图片均为豆包（Doubao）生成，最终交付为 800×800 JPG。映射如下：

| 文件 | 商品 | 生成源图（.codex-tmp/doubao/final800-*） |
| --- | --- | --- |
| goods-1.jpg | 华为 Mate 60 Pro | final800-mate-1.png |
| goods-2.jpg | 小米15 Pro | final800-xiaomi-1.png |
| goods-3.jpg | 纯棉基础T恤 | final800-tshirt-1.png |
| goods-4.jpg | Apple iPhone 15 Pro | final800-iphone-1.png |
| goods-5.jpg | 小米平板 6 Pro | final800-pad-1.png |
| goods-6.jpg | Apple MacBook Air M3 | final800-mac-a.png |
| goods-7.jpg | 美的 55英寸 4K 智能电视 | final800-tv-1.png |
| goods-8.jpg | 海尔 505L 十字对开门冰箱 | final800-fridge-1.png |
| goods-9.jpg | 优衣库 男装 防风连帽外套 | final800-jacket-1.png |
| goods-10.jpg | 女装 法式碎花连衣裙 | final800-dress-1.png |
| goods-11.jpg | 百丽 女士平底单鞋 | final800-shoe-1.png |
| goods-12.jpg | 悦木之源 灵芝焕能精华水 | final800-serum-1.png |
| goods-13.jpg | 完美日记 哑光唇釉 | final800-lip-1.png |
| goods-14.jpg | 三只松鼠 每日坚果礼盒 | final800-nut-1.png |
| goods-15.jpg | 智利车厘子 2斤装 | final800-cherry-1.png |

- 云库 `t_goods.main_image` 已全部指向 `/img/goods-N.jpg`（见 V3 迁移）。
- 首页横幅 `banner-1~3.jpg` 为前端 `home.js` 写死的本地静态运营位（1600×600）。已用项目内豆包成品图重制：品牌渐变底 + 右侧产品视觉 + 左侧文案/CTA，与商品卡同视觉语言。

## 6. 本轮改动清单（2026-09-05）

1. 商品图：确认豆包 15 张商品图生成完整（最终 800×800），用 `final800-*` 覆盖 `frontend/public/img/goods-1~15.jpg`；旧图备份在 `.codex-tmp/backup-goods/`。
2. 清理 `.codex-tmp/doubao`：删除 179 个中间文件（raw / fixed / tpl / final / bs / 遮罩 / 裁切 / 下载脚本等），仅保留 15 张 `final800-*`（生产源图）。
3. 初始化数据：新增 `backend/src/main/resources/db/migration/V3__init_seed_cloud.sql`（云库导出 + `INSERT IGNORE`），删除 `backend/src/main/java/com/dufeng/config/DataInitializer.java`。
4. 文档：更新 `backend/README.md` 演示账号说明；新增本进度文件。

## 7. 验证情况

- 后端编译：`mvn -o compile` 通过。
- 后端单测：`mvn -o test` → 28 个用例全部通过（Result / OrderStatus / OrderService / PaymentService / SmsCodeService / JwtUtil / LoginGuard）。
- 商品图：已目视核验 15 张主体与标题一致；`final800` 已将豆包“AI 生成”水印大部分去除（较旧批次更干净）。

## 8. 已知问题 / 待办

- 商品图个别左上角仍有极淡的“AI 生成”残余痕迹（豆包水印 + 此前 inpaint 处理），非阻断，已在卡片尺寸下不可见；如要求完全无痕需重新生成或精细修图。
- 前端 `admin.js` / `merchant.js` / `home.js` / `GoodsCard.vue` / `router/index.js` / `Home.vue` 有用户此前未提交的改动，本轮未触碰。
- 支付宝沙箱密钥、生产 `JWT_SECRET` / `PAY_SECRET` 等需按 `deploy/README.md` 配齐并走环境变量注入。
- 支付宝沙箱已接入并验证（见第 10 节）。剩余待办：DB/Redis 仍是弱密码 123456 需改强、后端以 root 运行需建独立用户、无域名/HTTPS 需绑定+证书、微信支付需真实商户资质。

## 9. 备注

- 云库中间件连接信息见 `backend/start-local.sh`（已被 gitignore，请勿提交密钥）。
- Git：当前分支 `master`；所有演示账号/数据均由云库与 V3 迁移提供，未硬编码。

## 10. 线上部署与支付宝沙箱（2026-09-05）

### 线上环境
- 站点：`http://119.29.167.133`（Nginx :80）；后端 `127.0.0.1:8080`（systemd `dufeng`），公网已测通。
- 服务器：CentOS 7；本机 `id_ed25519` 已写入 `root@119.29.167.133` 的 `authorized_keys`，现可免密 `ssh root@119.29.167.133`。
- 依赖：JDK 17（Temurin，`/opt/java/jdk17`）；Nginx 1.20.1（epel 安装）。
- 产物：后端 `/opt/dufeng/dufeng-ecommerce-backend.jar`；前端 `/opt/dufeng/dist`（含 15 商品图 + 3 横幅）。
- 配置：`/opt/dufeng/start.sh`（prod 环境变量：`DB=127.0.0.1:3306/dufeng`、`REDIS 6379:123456`、`JWT_SECRET`/`PAY_SECRET`、`ALIPAY_*`）；systemd 单元 `/etc/systemd/system/dufeng.service`；Nginx `/etc/nginx/conf.d/dufeng.conf`（`/api` 反代 `127.0.0.1:8080`）。
- 构建：后端 `cd backend && mvn clean package -DskipTests`；前端 `cd frontend && npm run build`。
- 登录分流（2026-09-05）：`Login.vue` 按角色直达——普通用户→`/`，商家→`/merchant`，管理员→`/admin`；有显式 `redirect`（如被拦截的结算页）则优先跳该页。
- 端隔离+界面分化（2026-09-05）：登录页增加身份选择（商城用户/商家/平台管理员）；商家端“渡风商家中心”（暖橙主题）、平台端“渡风平台管理中心”（深蓝主题）；路由商家端角色改为仅 `MERCHANT`，登录跳转只允许跳本角色区域，避免管理员被 `redirect=/merchant` 带偏。
- 登录页拆三端（2026-09-05）：`/login`（商城）、`/merchant/login`（商家中心）、`/admin/login`（平台管理中心）三个独立登录页，按路由 `meta.end` 切换标题/品牌/主题色；未登录拦截按目标跳对应登录页；两端登出走各自登录页。
- 后台观感打磨（2026-09-05）：两端布局新增全局后台样式（表格表头、卡片圆角/阴影、分页右对齐、行悬停、标签圆角），平台端工作台空图表补“暂无数据”空态。用户/商品等表格已有筛选栏+状态标签+分页+操作列。
- 商品审核数据修正（2026-09-05）：`GoodsQuery` 原无 `status/auditStatus`，筛选被后端忽略；已补字段并在 `pageQuery` 实现过滤，`GoodsVO` 增加 `brandName`。前端“商品审核”改按 `auditStatus` 筛选与展示（审核状态/商品状态标签、品牌名、销量），并修复 ID 列换行、按钮对齐。
- 购物车幂等（2026-09-05）：`CartServiceImpl.add` + `CartItemMapper` 新增 `restoreSoftDeleted`/`increaseQuantity`，修复“商品从购物车移除后（逻辑删除 `deleted=1`）再加购会撞 `uk_user_sku` 唯一键 → 系统繁忙”的 bug，已重打后端并上线验证（加购→移除→再加购均成功）。

### 支付宝沙箱
- 已配置并验证：`ALIPAY_ENABLED=true`；沙箱 APPID `9021000167651747`；网关 `https://openapi-sandbox.dl.alipaydev.com/gateway.do`；`ALIPAY_NOTIFY_URL=http://119.29.167.133/api/portal/pay/callback/alipay/notify`、`ALIPAY_RETURN_URL=http://119.29.167.133`。
- 密钥：应用私钥存服务器 `/opt/dufeng/alipay_app_private_key.txt`（单行）；应用公钥已上传沙箱应用；支付宝公钥已写入 `start.sh`。
- 验证：API 冒烟下单 `demo` → 发起 alipay 支付，正确返回 `openapi-sandbox...gateway.do?...&method=alipay.trade.page.pay...` 收银台地址（签名成功）。
- 真实付款：用沙箱「沙箱买家账号 + 支付密码」走站点收银台支付，不真实扣款。

> ⚠️ 2026-09-05 重配 `start.sh` 时重新生成了 `JWT_SECRET`/`PAY_SECRET`，此前登录 token 已全部失效，需重新登录。
